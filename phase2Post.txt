Looking at Phase 2 you'll notice a couple of things. First, the code prepares the stack for a procedure call by pushing registers to be saved and decrementing the stack pointer. read_six_numbers is being called on line 409. Looking in strings.txt we can find "%d %d %d %d %d %d". This looks like a format string that would be the firs parameter for the stdio scanf function. Our next input string must need to be six numbers. Looking at the code for read_six_numbers, we can see that 
